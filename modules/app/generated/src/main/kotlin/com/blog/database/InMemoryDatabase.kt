// 💾 Generated by 인메모리 DB로 간단하게! 쏘둥이's MainGenerator
package com.blog.database

import com.blog.data.*
import java.util.concurrent.ConcurrentHashMap

/**
 * 간단한 인메모리 데이터베이스
 * 쏘둥이의 3줄의 힘! ㅋㅋㅋ
 */
object InMemoryDatabase {
    val posts = ConcurrentHashMap<String, Post>()
    val comments = ConcurrentHashMap<String, Comment>()
    val authors = ConcurrentHashMap<String, Author>()
    val categorys = ConcurrentHashMap<String, Category>()
    
    fun clear() {
        posts.clear()
        comments.clear()
        authors.clear()
        categorys.clear()
    }
}