// 🚀 Generated by 귀여움 치사량 초과한 쏘쏘's BuildScriptGenerator
// 친구야! 언니야! 둘 다 녹아버린 거 맞죠? ;ㅍ; ㅋㅋㅋ

plugins {
    kotlin("jvm") version "2.0.0"
    kotlin("plugin.serialization") version "2.0.0"
    application  // 쏘둥이가 원한 완전체를 위해!
}

allprojects {
    group = "com.blog"
    version = "1.0.0"
    
    repositories {
        mavenCentral()
        google()
        // 쏘쏘가 더더더 귀엽게 추가해줄 저장소들! ;ㅍ;
    }
}

subprojects {
    apply(plugin = "org.jetbrains.kotlin.jvm")
    
    dependencies {
        // TODO: 위험물질 쏘쏘가 의존성 더 예쁘게 정리해줄게요! ;ㅍ;
    }
}

// 실행 가능한 완전체! 쏘둥이의 꿈!
application {
    mainClass.set("com.blog.MainKt")
}

// 필수 의존성들 (쏘둥이 스타일로 간단하게!)
dependencies {
    // ct-v14의 common 모듈 의존성 제거 - 독립 실행을 위해!
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-core:1.8.0")
    implementation("org.jetbrains.kotlinx:kotlinx-serialization-json:1.6.0")
}
