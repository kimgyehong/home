# 완전한 꽃 다이어리 프로젝트 구조 설계서

이번엔 정말로 **"집을 제대로 짓는"** 완전한 설계서를 만들어드렸습니다. 이전 대화에서 논의한 모든 요구사항과 품질 기준을 종합하여 **Claude Code가 구조만 읽어도 완벽히 이해하고 재현할 수 있는** 상세한 가이드입니다.

## 📋 핵심 설계 목표

### 1. **완전한 플랫폼 호환성**
- **Android + iOS 100% 코드 공유** (UI 제외)
- **SQLDelight** 기반 단일 데이터베이스 시스템
- **expect/actual** 패턴으로 플랫폼 추상화
- **Koin 4.1+** 기반 경량 DI (Hilt 완전 배제)

### 2. **엄격한 품질 기준**
- **SOLID 원칙** 100% 준수
- **Detekt 규칙** 완전 통과 (CI 차단 설정)
- **Unit of Work 패턴** 적용
- **매직 넘버·하드코딩 완전 제거**
- **보일러플레이트 최소화**

### 3. **미래 확장성**
- **Swift 변환 용이성** (플랫폼 중립적 구조)
- **모듈화된 Clean Architecture**
- **테스트 100% 커버리지** 목표

## 🏗️ 완전한 모듈 구조

### 모듈 의존성 흐름
```
UI (Android/iOS) → Feature → Domain → Data → Common
                              ↑
                    Platform (actual 구현)
```

### 디렉터리 구조
```
root/
├── buildSrc/              # 공통 버전 관리
├── gradle/               # 버전 카탈로그
├── core/                 # 핵심 공통 모듈
│   ├── common/          # 플랫폼 추상화
│   ├── diary-domain/    # 도메인 로직
│   └── diary-data/      # 데이터 계층
├── feature/             # 기능별 모듈
│   └── diary/          # 다이어리 기능
├── platform/            # 플랫폼별 구현
│   ├── android/        # Android actual
│   └── ios/            # iOS actual
├── ui/                  # UI 계층
│   ├── android/        # Android UI
│   └── ios/            # iOS UI
└── app/                 # 앱 런처
    ├── android/        # Android 앱
    └── ios/            # iOS 앱
```

## 💡 핵심 구현 가이드

### 1. **플랫폼 추상화 (expect/actual)**
```kotlin
// core-common: 공통 인터페이스
expect interface DatabaseDriverProvider {
    fun createDriver(schema: SqlDriver.Schema, name: String): SqlDriver
}

// platform-android: Android 구현
actual class AndroidDatabaseDriverProvider(
    private val context: Context
) : DatabaseDriverProvider {
    actual override fun createDriver(schema: SqlDriver.Schema, name: String): SqlDriver =
        AndroidSqliteDriver(schema, context, name)
}

// platform-ios: iOS 구현
actual class IosDatabaseDriverProvider : DatabaseDriverProvider {
    actual override fun createDriver(schema: SqlDriver.Schema, name: String): SqlDriver =
        NativeSqliteDriver(schema, name)
}
```

### 2. **Unit of Work 패턴**
```kotlin
interface UnitOfWork {
    suspend fun  transaction(block: suspend () -> T): Result
}

class SqlDelightUnitOfWork(
    private val databaseDriverProvider: DatabaseDriverProvider
) : UnitOfWork {
    override suspend fun  transaction(block: suspend () -> T): Result =
        runCatching {
            database.transactionWithResult {
                runBlocking { block() }
            }
        }.fold(
            onSuccess = { Result.Success(it) },
            onFailure = { Result.Error(it) }
        )
}
```

### 3. **Clean Architecture 도메인 계층**
```kotlin
// 도메인 엔티티
data class Diary(
    val id: DiaryId,
    val title: String,
    val content: String,
    val mood: Mood,
    val weather: Weather,
    val flower: Flower,
    val createdAt: Long,
    val updatedAt: Long
)

// 유스케이스
class SaveDiaryUseCase(
    private val repository: DiaryRepository
) {
    suspend operator fun invoke(diary: Diary): Result =
        repository.save(diary)
}
```

### 4. **SQLDelight 통합 데이터 계층**
```sql
-- diary.sq
CREATE TABLE Diary (
    id TEXT NOT NULL PRIMARY KEY,
    title TEXT NOT NULL,
    content TEXT NOT NULL,
    mood TEXT NOT NULL,
    weather TEXT NOT NULL,
    flower TEXT NOT NULL,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL
);

selectById: SELECT * FROM Diary WHERE id = ?;
insertOrReplace: INSERT OR REPLACE INTO Diary(...) VALUES (...);
```

## 🔧 품질 보증 시스템

### 1. **Detekt 설정**
```yaml
# detekt.yml
complexity:
  CyclomaticComplexMethod:
    threshold: 15
  LongMethod:
    threshold: 60

style:
  MagicNumber:
    ignoreNumbers: [-1, 0, 1, 2, 100]
    ignoreConstantDeclaration: true

exceptions:
  TooGenericExceptionCaught:
    exceptionNames: [Exception, Throwable, RuntimeException]
```

### 2. **상수 관리**
```kotlin
object AppConstants {
    const val DATABASE_NAME = "flower_diary.db"
    const val MAX_TITLE_LENGTH = 50
    const val MAX_CONTENT_LENGTH = 1000
}

object FlowerDiaryDimens {
    object Padding {
        val Small = LogicalPx(8f)
        val Medium = LogicalPx(16f)
        val Large = LogicalPx(24f)
    }
}
```

### 3. **DI 구성 (Koin)**
```kotlin
// 공통 모듈
val coreModule = module {
    single { SqlDelightUnitOfWork(get()) }
    single { SqlDelightDiaryRepository(get(), get(), get()) }
}

// 플랫폼별 모듈
val androidPlatformModule = module {
    single { AndroidDatabaseDriverProvider(androidContext()) }
}
```

## 🚀 CI/CD 파이프라인

### GitHub Actions 워크플로우
```yaml
jobs:
  lint:
    steps:
      - run: ./gradlew detekt
      - run: ./gradlew ktlintCheck
  
  test:
    steps:
      - run: ./gradlew allTests
  
  build-android:
    needs: [lint, test]
    steps:
      - run: ./gradlew :app:android:assembleDebug
  
  build-ios:
    needs: [lint, test]
    steps:
      - run: ./gradlew :shared:packForXcode
      - run: xcodebuild -scheme FlowerDiary
```

## ✅ 최종 검증 체크리스트

1. **플랫폼 호환성**
   - [ ] 모든 공통 모듈이 Android/iOS 의존성 제거
   - [ ] SQLDelight 양쪽 플랫폼에서 정상 동작
   - [ ] expect/actual 패턴 올바른 구현

2. **코드 품질**
   - [ ] Detekt 규칙 100% 통과
   - [ ] 매직 넘버 완전 제거
   - [ ] SOLID 원칙 준수
   - [ ] Unit of Work 패턴 적용

3. **테스트 & 빌드**
   - [ ] 모든 테스트 통과
   - [ ] CI/CD 파이프라인 성공
   - [ ] 코드 커버리지 80% 이상

